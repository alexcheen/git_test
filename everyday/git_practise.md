# git practise online
```url
http://learngitbranching.js.org
```

```git
git checkout master
git checkout -b foo
git checkout commit-id //HEAD detach

git rebase branchA branchB
// branchA <- branchB
git merge branchA branchB


git cherry-pick cid cid cid...
git rebase -i HEAD~3
git rebase -i master 
git tag git_tag commit_id
git describe
```

```git
git commit --amend
git rebase -i origin/master
git show-branch
git blame
git bisect
```